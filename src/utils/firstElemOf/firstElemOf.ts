/**
 * Gets the first element of an array
 * @param {any[]} array an array with at least one element
 * @returns {any} first element the array
 */

const firstElemOf = (array: any[]): any => array[0];

export default firstElemOf;
