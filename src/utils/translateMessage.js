// /* eslint-disable global-require */
// import { useIntl } from 'react-intl';
// import isDefined from './isDefined';

// const translateMessage = ({ id, values = {} }, messages = undefined, lang = undefined) => {
//   const teste = useIntl().formatMessage({ id: 'success.auction.create' });
//   debugger;
//   // const locale = lang || useIntl().locale;
//   // const hasValues = Object.keys(values).length;
//   // const translations = messages || {
//   //   pt: require('@esolidar/i18n/projects/toolkit/pt'),
//   //   en: require('@esolidar/i18n/projects/toolkit/en'),
//   //   br: require('@esolidar/i18n/projects/toolkit/br'),
//   // };
//   // let text = translations[locale][id] || id;

//   // if (hasValues && isDefined(text)) {
//   //   const regExp = new RegExp(Object.keys(values).join('|'), 'gi');
//   //   text = text.replace(regExp, matched => values[matched]);
//   // }

//   return id;
// };

// export default translateMessage;
